<Window x:Class="WpfTrain1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Demo First WPF"
        Height="520"
        Width="820"
        WindowStartupLocation="CenterScreen">

    <!-- STYLE VALIDATION -->
    <Window.Resources>
        <Style TargetType="TextBox">
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="True">
                    <Setter Property="Background" Value="#FFFFC0C0"/>
                    <Setter Property="ToolTip"
                            Value="{Binding RelativeSource={RelativeSource Self},
                                            Path=(Validation.Errors)[0].ErrorContent}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- FORM -->
        <StackPanel Orientation="Horizontal" Margin="0 0 0 10">

            <Label Content="Mã SP:" Width="60"/>
            <TextBox Width="120">
                <TextBox.Text>
                    <Binding Path="Ma"
                             UpdateSourceTrigger="PropertyChanged"
                             NotifyOnValidationError="True">
                        <Binding.ValidationRules>
                            <ExceptionValidationRule/>
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox.Text>
            </TextBox>

            <Label Content="Tên SP:" Width="70" Margin="10 0 0 0"/>
            <TextBox Width="180"
                     Text="{Binding Ten,
                                    UpdateSourceTrigger=PropertyChanged,
                                    NotifyOnValidationError=True,
                                    ValidatesOnDataErrors=True}"/>

            <Label Content="Giá:" Width="40" Margin="10 0 0 0"/>
            <TextBox Width="120">
                <TextBox.Text>
                    <Binding Path="Gia"
                             UpdateSourceTrigger="PropertyChanged"
                             NotifyOnValidationError="True">
                        <Binding.ValidationRules>
                            <ExceptionValidationRule/>
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox.Text>
            </TextBox>

        </StackPanel>

        <!-- BUTTON -->
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0 0 0 10">
            <Button Content="Thêm" Width="80" Click="BtnThem_Click"/>
            <Button Content="Sửa" Width="80" Margin="5 0" Click="BtnSua_Click"/>
            <Button Content="Xóa" Width="80" Click="BtnXoa_Click"/>
        </StackPanel>

        <!-- DATAGRID -->
        <DataGrid x:Name="dgSanPham"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  SelectionChanged="dgSanPham_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã SP" Binding="{Binding Ma}" Width="120"/>
                <DataGridTextColumn Header="Tên SP" Binding="{Binding Ten}" Width="*"/>
                <DataGridTextColumn Header="Giá" Binding="{Binding Gia}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- PAGING -->
        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0 10 0 0">
            <Button Content="◀ Trước" Width="80" Click="Prev_Click"/>
            <TextBlock x:Name="txtPage"
                       Margin="20 0"
                       FontWeight="Bold"
                       VerticalAlignment="Center"/>
            <Button Content="Sau ▶" Width="80" Click="Next_Click"/>
        </StackPanel>

    </Grid>
</Window>
